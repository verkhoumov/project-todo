// Основные параметры сайта.
var CONFIG = {
	// Breakpoint'ы размеров экрана.
	screenSizes: {
		customXs: 360,
		xs: 540,
		sm: 720,
		md: 960,
		lg: 1140
	},

	// Формат даты, хранимый в базе данных.
	dateFormat: 'YYYY-MM-DD HH:mm:ss'
};

module.exports = CONFIG;